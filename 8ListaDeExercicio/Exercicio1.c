#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]) {
    int x,r,cont;
    cont = 0;
    while(cont < 5) {
        printf("Digite um valor para X:");
        scanf("%d", &X);
        r = X * 3
        printf("\nO valor de r eh: %5d\n",r);
        cont ++;
    }
    system("PAUSE");
    return 0;
} 